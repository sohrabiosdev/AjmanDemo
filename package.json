{
  "name": "NavigatinSample",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "clean": "rimraf build && rimraf android/build && rimraf ios/build && rimraf android/app/build && rimraf node_modules && mkdir ios/build",
    "ios": "react-native run-ios"
  },
  "dependencies": {
    "react": "16.8.3",
    "react-native": "0.59.4",
    "react-navigation": "^3.6.1"
  },
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/runtime": "^7.4.3",
    "babel-jest": "^24.7.1",
    "jest": "^24.7.1",
    "metro-react-native-babel-preset": "^0.53.1",
    "react-test-renderer": "16.8.3"
  },
  "jest": {
    "preset": "react-native"
  }
}
